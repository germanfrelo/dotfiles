# TODO: **Implementation To-Do List:**

# 1. **Create the custom directory:** Make a new folder at `~/.config/zsh/custom/`.
# 2. **Create the split files:** Inside that folder, create `aliases.zsh`, `functions.zsh`, and `exports.zsh`.
# 3. **Migrate the logic:** Cut your personal blocks (`gbignore`, `syncfile`, `md`, `eza` aliases, `chezmoi` drift checker, etc.) from `~/.zshrc` and paste them into their respective new files.
# 4. **Source the files:** Add the `z4h source "${XDG_CONFIG_HOME}/zsh/custom/..."` lines to the very bottom of `~/.zshrc`.
# 5. **Track with chezmoi:** Run `chezmoi add ~/.config/zsh/custom/` to bring the new modular structure into your dotfiles repository.

# TODO: That is an incredibly valid concern. When you are building your personal environment on top of a heavily engineered framework like `zsh4humans`, dealing with upstream drift is exactly the kind of "thorn in the side" that plagues developers over time.
# 
# Here is an analysis of your current diff, followed by a strategic breakdown of how to solve this exact architectural problem.
# 
# ### 1. Re-evaluation of the Diff
# 
# I have carefully reviewed your `changes.diff` against the boilerplate `zsh4humans` (v5) `.zshrc` template.
# 
# **The Verdict:** **Your current diff is 100% safe to commit.** Here is why:
# 
# - **The User Space:** All the heavy modifications (the quotes, the braces, the `$(...)` command substitutions) in this diff are applied exclusively to _your_ custom functions (`syncfile`, `gbignore`), _your_ custom loop (`dirs`), and _your_ added conditional (`_chezmoi_status`).
# - **The WSL Line:** You correctly excluded the `z4h_win_home` line from this diff, leaving the author's strict Zsh-optimized assignment intact.
# - **The Boilerplate Placeholders:** The only native `zsh4humans` boilerplate you touched was `path=(~/bin ${path})`. The framework author provides these specific lines (along with headers like `# Define aliases` and `# Export environment variables`) precisely as designated "fill-in-the-blank" areas for you. Wrapping `${path}` in braces here does not break any framework logic.
# 
# ### 2. The `zsh4humans` Philosophy on Customization
# 
# Roman Perepelitsa (the author of `zsh4humans`) designed the default `.zshrc` not as an immutable black box, but as a scaffold.
# 
# According to the official documentation, the author explicitly recommends keeping all shell customization within this file or in files sourced from it. The design relies on one massive architectural rule: **The `z4h init` boundary.**
# 
# - **Above `z4h init`:** This is the critical framework configuration zone. You declare what plugins to download, what SSH hosts to teleport to, and UI behaviors. Console I/O is unavailable here, and tampering with the logic can break the fast-loading UI.
# 
# - **Below `z4h init`:** This is the standard Zsh environment. Once `z4h init` fires, the framework steps back, and the rest of the file operates like a normal, everyday `.zshrc`. The author provides the `# Define aliases` and `# Autoload functions` headers as suggested slots, expecting you to write your custom code there.
# 
# However, as you have noticed, dumping hundreds of lines of custom functions into the bottom of this scaffold makes it incredibly difficult to track what belongs to you versus what came from the template.
# 
# ### 3. Solutions for Absolute Separation
# 
# If you want absolute peace of mind when pulling upstream changes from the `zsh4humans` repository, you need to physically separate the code. Here are the two best approaches.
# 
# #### Strategy A: The Modular "Source" Approach (Highly Recommended)
# 
# This is the cleanest, most professional way to organize dotfiles. Instead of using the `zsh4humans` `.zshrc` as your main coding canvas, you treat it purely as a bootloader.
# 
# 1. **Extract your code:** You create a new directory, for example, `~/.config/zsh/custom/`. Inside it, you create dedicated files like `aliases.zsh`, `functions.zsh`, and `exports.zsh`.
# 2. **Move the logic:** You cut all your custom code (`syncfile`, `md`, `eza` aliases, `chezmoi` drift checker) out of `.zshrc` and paste them into these new files.
# 3. **Source them in `.zshrc`:** At the very bottom of your `.zshrc`, you add a few clean lines:
# 
# ```zsh
# # ==================================================
# # Load Personal Customizations
# 
# z4h source "${XDG_CONFIG_HOME}/zsh/custom/exports.zsh"
# z4h source "${XDG_CONFIG_HOME}/zsh/custom/functions.zsh"
# z4h source "${XDG_CONFIG_HOME}/zsh/custom/aliases.zsh"
# 
# ```
# 
# **Why this wins:** Your `.zshrc` remains almost identical to the upstream `zsh4humans` template. If Roman pushes an update, you can effortlessly diff your `.zshrc` against the new boilerplate without your personal aliases getting in the way. Since you use `chezmoi`, managing these extra files in your `private_dot_config/zsh/custom/` directory is seamless.
# 
# #### Strategy B: The "Block Demarcation" Approach
# 
# If you strongly prefer keeping everything in a single file to avoid jumping between tabs in VS Code, you can use visual boundaries.
# 
# You leave the upper half of the file untouched. Right below `z4h init`, you wipe out the author's empty placeholder headers and replace them with a massive, unmissable ASCII block:
# 
# ```zsh
# # ==============================================================================
# # ⬇⬇⬇ END OF ZSH4HUMANS BOILERPLATE ⬇⬇⬇
# # ==============================================================================
# #
# # Everything below this line is strictly personal configuration.
# # Upstream zsh4humans changes should only be compared above this point.
# 
# export EDITOR="code --wait"
# # ... rest of your code ...
# 
# ```
# 
# **Why this wins:** It requires zero file restructuring. You maintain the single-file convenience while giving yourself a psychological and visual "safe zone" where you know you can edit, format, and refactor without ever worrying about breaking framework initialization.
# 
# ---
# 
# Would you like me to draft the exact file split for the Modular Approach (Strategy A) so you can see what your stripped-down `.zshrc` and your new custom files would look like?
#
# =======================================================================
# Personal Zsh configuration file. It is strongly recommended to keep all
# shell customization and configuration (including exported environment
# variables such as PATH) in this file or in files sourced from it.
#
# Documentation: https://github.com/romkatv/zsh4humans/blob/v5/README.md

# =======================================================================

# Periodic auto-update on Zsh startup: 'ask' or 'no'.
# You can manually run `z4h update` to update everything.
zstyle ':z4h:' auto-update      'ask'
# Ask whether to auto-update this often; has no effect if auto-update is 'no'.
zstyle ':z4h:' auto-update-days '7'

# Keyboard type: 'mac' or 'pc'.
zstyle ':z4h:bindkey' keyboard  'mac'

# Start tmux if not already in tmux.
zstyle ':z4h:' start-tmux command tmux -u new -A -D -t z4h

# Whether to move prompt to the bottom when zsh starts and on Ctrl+L.
zstyle ':z4h:' prompt-at-bottom 'yes'

# Mark up shell's output with semantic information.
zstyle ':z4h:' term-shell-integration 'yes'

# Right-arrow key accepts one character ('partial-accept') from
# command autosuggestions or the whole thing ('accept')?
zstyle ':z4h:autosuggestions' forward-char 'accept'

# Recursively traverse directories when TAB-completing files.
zstyle ':z4h:fzf-complete' recurse-dirs 'no'

# Enable direnv to automatically source .envrc files.
zstyle ':z4h:direnv'         enable 'yes'
# Show "loading" and "unloading" notifications from direnv.
zstyle ':z4h:direnv:success' notify 'yes'

# Enable ('yes') or disable ('no') automatic teleportation of z4h over
# SSH when connecting to these hosts.
zstyle ':z4h:ssh:example-hostname1'   enable 'yes'
zstyle ':z4h:ssh:*.example-hostname2' enable 'no'
# The default value if none of the overrides above match the hostname.
zstyle ':z4h:ssh:*'                   enable 'no'

# Send these files over to the remote host when connecting over SSH to the
# enabled hosts.
zstyle ':z4h:ssh:*' send-extra-files '~/.nanorc' '~/.env.zsh'

# ==================================================

# Clone additional Git repositories from GitHub.
#
# This doesn't do anything apart from cloning the repository and keeping it
# up-to-date. Cloned files can be used after `z4h init`. This is just an
# example. If you don't plan to use Oh My Zsh, delete this line.
# z4h install ohmyzsh/ohmyzsh || return

# ==================================================

# Install or update core components (fzf, zsh-autosuggestions, etc.) and
# initialize Zsh. After this point console I/O is unavailable until Zsh
# is fully initialized. Everything that requires user interaction or can
# perform network I/O must be done above. Everything else is best done below.
z4h init || return

# ==================================================

# Extend PATH.
path=(~/bin $path)

# ==================================================

# Export environment variables.

# Preferred editor for CLI tools.
export EDITOR="code --wait"
export VISUAL="code --wait"

# GPG TTY for passphrase prompts
export GPG_TTY=$TTY

# User directories (XDG Base Directory Specification)
# Ref. https://wiki.archlinux.org/title/XDG_Base_Directory#User_directories
export XDG_CACHE_HOME="${HOME}/.cache"
export XDG_CONFIG_HOME="${HOME}/.config"
export XDG_DATA_HOME="${HOME}/.local/share"
export XDG_STATE_HOME="${HOME}/.local/state"

# Homebrew
export HOMEBREW_BUNDLE_FILE="${XDG_CONFIG_HOME}/homebrew/Brewfile"
export HOMEBREW_NO_ANALYTICS=1

# NVM (Node Version Manager)
# Ref: https://github.com/romkatv/zsh4humans/issues/351
export NVM_DIR="${HOME}/.nvm"

# man / less
export LESSHISTFILE=- # avoid showing .lesshst file in $HOME when using `man' command

# ==================================================

# Source additional local files if they exist.

z4h source ~/.env.zsh

# Ref: https://github.com/romkatv/zsh4humans/issues/351
z4h source "${NVM_DIR}/nvm.sh" "${NVM_DIR}/bash_completion"

# ==================================================

# Use additional Git repositories pulled in with `z4h install`.
#
# This is just an example that you should delete. It does nothing useful.
# z4h source ohmyzsh/ohmyzsh/lib/diagnostics.zsh  # source an individual file
# z4h load   ohmyzsh/ohmyzsh/plugins/emoji-clock  # load a plugin

# ==================================================

# Define key bindings.

z4h bindkey undo Ctrl+/   Shift+Tab  # undo the last command line change
z4h bindkey redo Option+/            # redo the last undone command line change

z4h bindkey z4h-cd-back    Shift+Left   # cd into the previous directory
z4h bindkey z4h-cd-forward Shift+Right  # cd into the next directory
z4h bindkey z4h-cd-up      Shift+Up     # cd into the parent directory
z4h bindkey z4h-cd-down    Shift+Down   # cd into a child directory

# ==================================================

# Autoload functions.

autoload -Uz zmv

# ==================================================

# Define functions and completions.

# Create a new directory and instantly navigate into it (with folder tab-completion)
function md() { [[ $# == 1 ]] && mkdir -p -- "$1" && cd -- "$1"; }
compdef _directories md

# Append the HEAD commit hash to .git-blame-ignore-revs.
# Only runs if the commit title matches style*:* (case-insensitive).
# Skips automatically if the commit is itself a .git-blame-ignore-revs update.
function gbignore() {
	local commit_hash commit_title title_lower

	commit_hash="$(git rev-parse HEAD 2>/dev/null)" || {
		print -P "%F{red}Error: not a git repository or no commits.%f" >&2
		return 1
	}
	commit_title="$(git log -1 --format=%s)"
	title_lower="${commit_title:l}"

	case "${title_lower}" in
		*git-blame-ignore-revs*)
			print -P "%F{yellow}Skipped: commit appears to be a .git-blame-ignore-revs update.%f" >&2
			return 1
			;;
	esac

	case "${title_lower}" in
		style*:*) ;;
		*)
			print -P "%F{red}Aborted: \"%s\" does not match style*:*.%f" "${commit_title}" >&2
			return 1
			;;
	esac

	if [[ ! -f .git-blame-ignore-revs ]]; then
		print -P "%F{red}Error: .git-blame-ignore-revs not found in %~.%f" >&2
		return 1
	fi

	if grep -qF "${commit_hash}" .git-blame-ignore-revs; then
		print -P "%F{yellow}Skipped: %s is already in .git-blame-ignore-revs.%f" "${commit_hash}" >&2
		return 0
	fi

	printf "\n# %s\n%s\n" "${commit_title}" "${commit_hash}" >> .git-blame-ignore-revs
	git add .git-blame-ignore-revs
	git commit -m "chore: Update .git-blame-ignore-revs"
	print -P "%F{green}Done: added %s to .git-blame-ignore-revs.%f" "${commit_hash}"
}

# Copies a file to all subdirectories in the current path, excluding the source directory.
# Usage: syncfile path/to/source/.gitattributes
syncfile() {
	local src_file="$1"

	# 1. Validation: Ensure source file exists
	if [[ ! -f "${src_file}" ]]; then
		echo "🚫 Error: Source file '${src_file}' not found."
		return 1
	fi

	# 2. Extraction: Get the filename and the absolute path of its parent directory
	local filename="$(basename "${src_file}")"
	local src_dir_abs="$(cd "$(dirname "${src_file}")" && pwd)"

	echo "🚀 Syncing '${filename}' to all repositories (excluding origin)..."

	# 3. Execution: Loop through all directories
	for dir in */; do
		# Remove trailing slash for path comparison
		local target_dir="${dir%/}"
		local target_dir_abs="$(cd "${target_dir}" && pwd)" || continue

		# Skip the directory that contains the source file
		if [[ "${target_dir_abs}" == "${src_dir_abs}" ]]; then
			echo "🏠 Skipping source: ${target_dir}"
			continue
		fi

		# Only sync to directories that are actually Git repositories (optional, but safer for you)
		if [[ -d "${target_dir}/.git" ]]; then
			cp "${src_file}" "${target_dir}/${filename}"
			echo "✅ Synced to: ${target_dir}"
		fi
	done
}

# ==================================================

# Define named directories: ~w <=> Windows home directory on WSL.

[[ -z $z4h_win_home ]] || hash -d w=$z4h_win_home

# ==================================================

# Define aliases.

# Files and directories

# Recently visited directories
# List
alias dirs="dirs -v"
# Quick access by index
for index in {1..9}; do alias "${index}"="cd +${index}"; done
unset index

# Quick-access directories
alias repos="${HOME}/repos/germanfrelo"

# List all files and directories in columns
alias eza="eza -F --all --group-directories-first"
# Same as above, but displaying extended metadata as a table
alias ezal="eza --long --header --smart-group --flags --time-style=iso --no-user --git-repos"
# Show the contents of the current directory as a tree, including hidden files and directories
alias tree="tree -a"
alias tree1="tree -L 1"
alias tree2="tree -L 2"
alias tree3="tree -L 3"

# File contents
alias bat="bat --paging=never"

# Find patterns in files using regular expressions
alias grep="grep -i"

# Actions on files and directories
alias cp="cp -iv"
alias mv="mv -iv"
alias rm="rm -iv"

# Git
alias gl="git log --oneline --graph"
alias gla="gl --all"
alias gls="gl --stat"
alias glt="git log --graph --pretty=format:'%C(yellow)%h%Creset%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)%an%Creset'"
alias gunstage="git restore --staged ." # restore the index
alias gstashu="git stash --include-untracked"
alias gstat="git status -sb"
# https://github.com/Phantas0s/.dotfiles/blob/master/aliases/aliases
# alias gs='git status'
# alias gss='git status -s'
# alias ga='git add'
# alias gp='git push'
# alias gpraise='git blame'
# alias gpo='git push origin'
# alias gpof='git push origin --force-with-lease'
# alias gpofn='git push origin --force-with-lease --no-verify'
# alias gpt='git push --tag'
# alias gtd='git tag --delete'
# alias gtdr='git tag --delete origin'
# alias grb='git branch -r' # display remote branch
# alias gplo='git pull origin'
# alias gb='git branch '
# alias gc='git commit'
# alias gd='git diff'
# alias gco='git checkout '
# alias gl='git log --oneline'
# alias gr='git remote'
# alias grs='git remote show'
# alias glol='git log --graph --abbrev-commit --oneline --decorate'
# alias gclean="git branch --merged | grep  -v '\\*\\|master\\|develop' | xargs -n 1 git branch -d" # Delete local branch merged with master
# alias gblog="git for-each-ref --sort=committerdate refs/heads/ --format='%(HEAD) %(color:red)%(refname:short)%(color:reset) - %(color:yellow)%(objectname:short)%(color:reset) - %(contents:subject) - %(authorname) (%(color:blue)%(committerdate:relative)%(color:reset))'" # git log for each branches
# alias gsub="git submodule update --remote" # pull submodules
# alias gj="git-jump" # Open in vim quickfix list files of interest (git diff, merged...)

# wget (XDG location)
alias wget='wget --hsts-file="${XDG_CACHE_HOME}/wget-hsts"'

# ==================================================

# Add flags to existing aliases.

alias ls="${aliases[ls]:-ls} -A"

# ==================================================

# Set shell options: https://zsh.sourceforge.io/Doc/Release/Options.html

setopt glob_dots     # no special treatment for file names with a leading dot
setopt no_auto_menu  # require an extra TAB press to open the completion menu

# ==================================================

# Warn if any chezmoi-managed file has drifted from its source.
# Runs once per top-level shell (SHLVL=1) — skipped in subshells and nested zsh invocations.
if (( SHLVL == 1 )); then
    _chezmoi_status="$(chezmoi status 2>/dev/null)"
    if [[ -n "${_chezmoi_status}" ]]; then
        print -P "%F{yellow}⚠ chezmoi: managed files have drifted — run \`chezmoi diff --use-builtin-diff\` to review:%f"
        print "${_chezmoi_status}"
    fi
    unset _chezmoi_status
fi
